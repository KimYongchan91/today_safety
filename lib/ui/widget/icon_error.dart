import 'package:flutter/material.dart';

class IconError extends StatelessWidget {
  const IconError({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Icon(
      Icons.error,
      color: Colors.red,
    );
  }
}
